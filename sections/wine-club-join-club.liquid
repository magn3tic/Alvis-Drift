<section class="container">
  <script>
        const shopDomain = 'alvis-drift.myshopify.com';
      
        function getCustomerIdAndFetchPoints() {
          const customerId = window.BOLD?.common?.Shopify?.customer?.id;
      
          if (customerId) {
            fetch(`/apps/loyalties-widget/v2/${shopDomain}/frontend/customer-tiers-list?shopifyCustomerId=${customerId}`)
              .then(r => r.json())
              .then(data => {
                console.log("✅ Puntos actuales del cliente:", data.points_balance);
              })
              .catch(err => console.error("❌ Error al obtener los puntos:", err));
          } else {
            console.warn("🕒 Esperando a que BOLD cargue o el cliente se autentique...");
            setTimeout(getCustomerIdAndFetchPoints, 500); // Reintenta en 0.5s
          }
        }
      
        getCustomerIdAndFetchPoints();
  </script>

</section>
{% schema %}
  {
    "name": "Wine Club join club",
    "settings": [],
    "presets": [
      {
        "name": "Wine Club join club",
        "category": "Wine Club"
      }
    ]
  }

{% endschema %}